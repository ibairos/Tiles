package model;

import constants.Enums;

public class UnknownCardModel extends CardModel {
    public UnknownCardModel(int cardNumber, int index, Enums.CardState state) {
        super(cardNumber, index, state);
    }
}
