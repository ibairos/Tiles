package model.card;

import constants.Enums;

public class CustomCardModel extends CardModel {
    public CustomCardModel(int cardNumber, int index, Enums.CardState state) {
        super(cardNumber, index, state);
    }
}
